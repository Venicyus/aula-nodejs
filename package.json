{
  "name": "aula-nodejs",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .build",
    "start": "clear && npm run clean && npm run build && node .build/src/app.js"
  },
  "dependencies": {
    "axios": "1.7.9"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.7.3"
  }
}
